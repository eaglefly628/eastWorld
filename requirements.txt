# Core dependencies (vendored in vendor/, no install needed)
# Just run: python run.py
#
# To use Claude API for NPC intelligence:
#   pip install anthropic
#   EASTWORLD_BACKEND=claude ANTHROPIC_API_KEY=sk-... python run.py
